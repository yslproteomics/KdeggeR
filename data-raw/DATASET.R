## code to prepare `DATASET` dataset goes here

usethis::use_data(example_spectronaut, overwrite = TRUE)

usethis::use_data(example_spectronaut_design, overwrite = TRUE)

usethis::use_data(example_spectronaut_design_replicates, overwrite = TRUE)
